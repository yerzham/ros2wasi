diff --git a/CMakeLists.txt b/CMakeLists.txt
index 539c4bf..2e1e900 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -22,6 +22,11 @@ ament_vendor(spdlog_vendor
     -DSPDLOG_BUILD_TESTS:BOOL=OFF
     -DSPDLOG_BUILD_EXAMPLE:BOOL=OFF
     "-DCMAKE_CXX_FLAGS=${CMAKE_CXX_FLAGS}"
+    "-DCMAKE_C_FLAGS=${CMAKE_C_FLAGS}"
+    "-DCMAKE_EXE_LINKER_FLAGS=${CMAKE_EXE_LINKER_FLAGS}"
+    "-DCMAKE_SHARED_LINKER_FLAGS=${CMAKE_EXE_LINKER_FLAGS}"
+  PATCHES
+    wasi_link_fix.patch
 )
 
 if(BUILD_TESTING)

# Untracked files:
# New file: wasi_link_fix.patch
diff --git a/wasi_link_fix.patch b/wasi_link_fix.patch
new file mode 100644
--- /dev/null
+++ b/wasi_link_fix.patch
@@ -0,0 +1,19 @@
+--- a/CMakeLists.txt
++++ b/CMakeLists.txt
+@@ -183,6 +183,16 @@ target_compile_definitions(spdlog PUBLIC SPDLOG_COMPILED_LIB)
+ target_include_directories(spdlog ${SPDLOG_INCLUDES_LEVEL} PUBLIC "$<BUILD_INTERFACE:${CMAKE_CURRENT_LIST_DIR}/include>"
+                                                                   "$<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>")
+ target_link_libraries(spdlog PUBLIC Threads::Threads)
++
++# WASI requires explicit linking of unwind and emulated libraries
++if(CMAKE_SYSTEM_NAME STREQUAL "WASI")
++    target_link_libraries(spdlog PRIVATE
++        /opt/wasi-sdk/share/wasi-sysroot/lib/wasm32-wasip2/libc++abi.a
++        /opt/wasi-sdk/share/wasi-sysroot/lib/wasm32-wasip2/libunwind.a
++        /opt/wasi-sdk/share/wasi-sysroot/lib/wasm32-wasip2/libwasi-emulated-mman.so
++        /opt/wasi-sdk/share/wasi-sysroot/lib/wasm32-wasip2/libwasi-emulated-getpid.so
++        /opt/wasi-sdk/share/wasi-sysroot/lib/wasm32-wasip2/libwasi-emulated-process-clocks.so)
++endif()
+ spdlog_enable_warnings(spdlog)
+ 
+ set_target_properties(spdlog PROPERTIES VERSION ${SPDLOG_VERSION} SOVERSION
